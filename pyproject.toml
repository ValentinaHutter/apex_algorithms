[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "esa-apex-algorithms"
dynamic = ["version"]
authors = [
  { name="Stefaan Lippens", email="stefaan.lippens@vito.be" },
]
description = "Python toolbox to interact with ESA APEx algorithm catalog."
readme = "README.md"
license = {file = "LICENSE"}
requires-python = ">=3.8"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: Apache Software License",
]
keywords = ["ESA", "EO", "Earth observation", "openEO"]
dependencies = [
    "requests>=2.30.0",
]

[project.urls]
Homepage = "https://github.com/ESA-APEx/apex_algorithms"
Issues = "https://github.com/ESA-APEx/apex_algorithms/issues"

[project.optional-dependencies]
tests = [
    "pytest>=8.2.0",
    "requests_mock>=1.12.0",
    "jsonschema",
]
dev = [
    "pytest>=8.2.0",
    "requests_mock>=1.12.0",
]

[tool.hatch.version]
path = "src/esa_apex_toolbox/_about.py"

[tool.hatch.build.targets.wheel]
packages = ["src/esa_apex_toolbox"]

[tool.black]
line-length = 120

[tool.isort]
profile = "black"

[tool.ruff]
line-length = 120

[tool.pytest.ini_options]
testpaths = [
    "tests",
]
